Blocked All Port in firewall (ekran backend server)

Go to "Control Panel" or Type "wf.msc" in run.

On "Windows Defender Firewall with Advance Security" click "Outbound Rules"

Click "New Rule"

Click "Port"

Select "UDP" on Specific remote ports: Type "0-5000". Then click "next"

Select "Block the connection" click "Next"

Click "Next"

Type any "Name", click "Finish.
